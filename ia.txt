etapa 1
promp: importa recetas.js a app.jsx y crea una conección con console.log
muy util

etapa 2
promp: crea el archivo RecetasCard.jsx en src/components, recibe los props de recetas.js y muestra la información con estilos diferenciados para fondo, entrada y postre
cambios: componente `RecetasCard` creado con estilos inline, importado y usado en `App.jsx` para renderizar cada receta

Etapa 3
promp: crea ListaRecetas.jsx en components y haz que reciba el array de recetas por props, luego recorre el array con .map() y renderiza un RecetasCard por cada receta. Asigna correctamente la propkey usando el id de cada elemento asegurate de un usar el indice del array, renderiza ListaRecetas pasando los datos importantes en App.jsx y agrega la informacion de los cambios a ia.txt EN EL BLOQUE DE LA ETAPA 3
cambios: componente `ListaRecetas` creado en `src/components/ListaRecetas.jsx`, recibe `recetas` por props y renderiza un `RecetasCard` por elemento usando `receta.id` como `key`; `App.jsx` actualizado para importar y usar `ListaRecetas` con el array `recetas`.

Etapa 4
promp: crea FiltroCategoria.jsx en components con botones o un selector para filtrar los las categorias existentes. utiliza el usestate para guardar el filtro seleccionado y un campo de busqueda por nombre en app.jsx, aplica ambos filtros sobre el array antes de pasarlo a ListaRecetas. utiliza el renderizado condicional para mostrar el mensaje "No hay recetas que coincidan" cuando el resultado de la busqueda quede vacio y agrega los cambios a ia.txt en el bloque de la etapa 4
cambios: componente `FiltroCategoria` creado en `src/components/FiltroCategoria.jsx`; `App.jsx` actualizado con estado `categoriaSeleccionada` y `terminoBusqueda`, filtros aplicados antes de pasar `recetasFiltradas` a `ListaRecetas`, y renderizado condicional para mostrar "No hay recetas que coincidan" cuando no hay resultados.

